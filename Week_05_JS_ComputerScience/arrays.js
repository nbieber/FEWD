var myArray = ["thing 1", "thing 2", "thing 3"];

var rainbowColors = ['Red', 'Orange', 'Yellow', 'Green', 'Blue', 'Indigo', 'Violet'];
var myFavoriteThings = ['Broccoli', 60481, 'Love Actually'];

var names = ["Nicole", "Sylvain", "Victoria", "Nick"];
names.push('Codi'); // Add to end
names.unshift('Sam'); // Add to front
names.splice(0, 1); // Remove elements
console.log(names.length); // print out the length
console.log(names[4]); // print item 4 (remember we start counting at 0)
names[3] = "Nick (again)"; // replace something


// Make an array of strings

// Add to it

// Remove something from it 

// Replace something

// Print it out to the console
